export default function TrustBar() {
  return (
    <section className="trustbar">
      <div className="wrap">
        <p className="big" data-reveal="">
          The semantic layer is fast becoming the standard interface between
          enterprise data and AI, the layer that decides whether your agents
          answer with <span className="num">the truth</span> or a guess.
        </p>
      </div>
    </section>
  );
}
