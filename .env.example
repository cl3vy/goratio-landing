# Optional: where lead submissions are forwarded (POST JSON).
# Point this at your CRM, a Zapier/Make hook, a serverless function, etc.
# If unset, the /api/lead endpoint logs submissions server-side instead.
LEAD_WEBHOOK_URL=
